command_timeout = 3600

"$schema" = 'https://starship.rs/config-schema.json'

add_newline = true

format = """
 ➜\
$directory\
${custom.giturl}\
$git_branch\
$git_state\
$git_status\
$line_break\
$character
"""

right_format = """$all"""

palette = "custom"

[palettes.custom]
base = "#1a1b26"       # Tokyo Night Storm Background
mantle = "#16161e"     # Darker Background
crust = "#11111b"      # Darkest Background
text = "#c0caf5"       # Foreground
subtext1 = "#a9b1d6"
subtext0 = "#9aa5ce"

red = "#f7768e"
orange = "#ff9e64"
yellow = "#e0af68"
green = "#9ece6a"
teal = "#73daca"
sky = "#7dcfff"
blue = "#7aa2f7"
purple = "#bb9af7"

sapphire = "#7aa2f7"   # Maps to Blue (Used for Directory)
lavender = "#9d7cd8"   # Maps to Purple (Used for Git Branch)
peach = "#ff9e64"      # Maps to Orange (Used for Time BG)
foam = "#7dcfff"       # Maps to Cyan (Used for Time FG)
pine = "#2ac3de"       # Maps to Cyan-Dark
maroon = "#f7768e"     # Maps to Red
pink = "#bb9af7"       # Maps to Magenta
flamingo = "#f7768e"   # Maps to Red
rosewater = "#f7768e"  # Maps to Red
gold = "#e0af68"       # Maps to Yellow
rose = "#f7768e"       # Maps to Red
black = "#15161e"      # Dark Black (Used for Git Branch pill background)


[os.symbols]
Windows = "󰍲"
Ubuntu = "󰕈"
SUSE = ""
Raspbian = "󰐿"
Mint = "󰣭"
Macos = " "
Manjaro = ""
Linux = "󰌽"
Gentoo = "󰣨"
Fedora = "󰣛"
Alpine = ""
Amazon = ""
Android = ""
Arch = "󰣇"
Artix = "󰣇"
CentOS = ""
Debian = "󰣚"
Redhat = "󱄛"
RedHatEnterprise = "󱄛"

[username]
# show_always = true
# style_user = "bg:surface0 fg:text"
# style_root = "bg:surface0 fg:text"
# format = '[ $user ]($style)'

[directory]
style = "sapphire"
format = "[ $path ]($style)"
#truncation_length = 4
#truncation_symbol = "…/"

[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
"Developer" = "󰲋 "
"Others" = " "

[os]
disabled = true
#style = "bg:surface0 fg:text"

[custom.giturl]
# disabled = true
description = "Display symbol for remote Git server"
command = """
GIT_REMOTE=$(command git ls-remote --get-url 2> /dev/null)
if [[ "$GIT_REMOTE" =~ "github" ]]; then
    GIT_REMOTE_SYMBOL=" "
elif [[ "$GIT_REMOTE" =~ "gitlab" ]]; then
    GIT_REMOTE_SYMBOL=" "
elif [[ "$GIT_REMOTE" =~ "bitbucket" ]]; then
    GIT_REMOTE_SYMBOL=" "
elif [[ "$GIT_REMOTE" =~ "git" ]]; then
    GIT_REMOTE_SYMBOL=" "
else
    GIT_REMOTE_SYMBOL=" "
fi
echo "$GIT_REMOTE_SYMBOL "
"""
when = 'git rev-parse --is-inside-work-tree 2> /dev/null'
format = "at $output"

[git_branch]
symbol = "[](black) "
# format =  ' [$symbol$branch(:$remote_branch)]($style)[]'
style = "fg:lavender bg:black"
format =  '  on [$symbol$branch]($style)[](black)'

[git_status]
format = ' [($all_status$ahead_behind )]($style)'

[nodejs]
symbol = ""
format = '[ $symbol( $version) ]($style)'

[c]
symbol = " "
format = '[ $symbol( $version) ]($style)'

[rust]
symbol = ""
format = '[ $symbol( $version) ]($style)'

[golang]
symbol = ""
format = '[ $symbol( $version) ]($style)'
detect_files = ["go.mod"]

[php]
symbol = ""
format = '[ $symbol( $version) ]($style)'

[java]
symbol = " "
format = '[ $symbol( $version) ]($style)'

[kotlin]
symbol = ""
format = '[ $symbol( $version) ]($style)'

[python]
symbol = ""
format = '[$symbol$version]($style)'

[docker_context]
symbol = ""
format = '[ $symbol( $context) ]($style)'

[time]
disabled = true
time_format = "%R"
style = "bg:peach"
format = '[[  $time ](fg:mantle bg:foam)]($style)'

[line_break]
disabled = true

[character]
disabled = false
success_symbol = '[ ](bold fg:green)'
error_symbol = '[✘ ](bold fg:red)'
vimcmd_symbol = "[ :](bold yellow)"
vimcmd_replace_one_symbol = "[󰝿 :](bold purple)"
vimcmd_replace_symbol = "[  :](bold purple)"
vimcmd_visual_symbol = "[  :](bold cyan)"
